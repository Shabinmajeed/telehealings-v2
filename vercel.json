{
  "$schema": "https://vercel.com/docs/project-configuration",
  "buildCommand": "cd packages/web-admin && npx tsc --noEmit && npx vite build",
  "outputDirectory": "packages/web-admin/dist",
  "framework": "vite",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
